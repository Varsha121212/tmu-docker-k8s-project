[project]
name = "bookstore-inventory-service"
version = "0.1.1"
description = "Stage 2/3 Inventory microservice for the CN8001 Online Bookstore project"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "sqlalchemy>=2.0",
    "alembic>=1.14",
    "psycopg[binary]>=3.2",
    # Not used by the app itself (Inventory makes no outbound calls) - only by
    # scripts/seed_stock.py, which reads the book list from catalog-service.
    "httpx>=0.27",
    "prometheus-fastapi-instrumentator>=8.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
